# Workspace

This directory contains controlled demos and experiments. Nothing here is a
production runtime dependency unless a component README explicitly says so.

- `demo/` is the owned storefront served by the local Go service.
- `scripts/` contains browser-console, harness, smoke, and historical scripts.
- `sqlite-data/` preserves the pre-Neon local database files and remains ignored.
