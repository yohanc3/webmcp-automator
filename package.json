{
  "name": "webmcp-automator",
  "version": "0.1.0",
  "private": true,
  "description": "Learn deterministic WebMCP adapters from demonstrated browser workflows.",
  "scripts": {
    "start": "set -a; [ -f .env ] && . ./.env; set +a; cd backend && go run ./cmd/server",
    "smoke:discover": "node scripts/discovery-smoke.js",
    "test": "node --test && cd backend && go test ./...",
    "check": "node --check extension/manifest-contract.js && node --check extension/recorder-core.js && node --check extension/background.js && node --check extension/semantic.js && node --check extension/runner.js && node --check extension/content.js && node --check extension/popup.js && node --check demo/app.js && node --check scripts/discovery-smoke.js && node --check scripts/browser-recorder-harness.js && cd backend && go vet ./..."
  }
}
